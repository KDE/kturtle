require File.dirname(__FILE__) + '/spec_helper.rb'
$i = Interpreter.instance

describe "boolean operator" do
  it "should work as expected with basic operations (and, or, not)"
  it "should raise an error when insufficient values are provided"
end